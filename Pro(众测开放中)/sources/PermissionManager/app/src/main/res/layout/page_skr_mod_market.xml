<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:paddingStart="10dp"
    android:paddingEnd="10dp"
    android:paddingBottom="10dp"
    android:paddingTop="0dp">

    <!-- 搜索框（用于筛选） -->
    <EditText
        android:id="@+id/market_search_edit"
        android:layout_width="match_parent"
        android:layout_height="44dp"
        android:hint="搜索模块名称 / 作者 / 关键词"
        android:imeOptions="actionSearch"
        android:inputType="text"
        android:singleLine="true"
        android:gravity="center_vertical"

        android:textSize="14sp"
        android:textColor="#303133"
        android:textColorHint="#9AA0A6"
        android:includeFontPadding="false"

        android:paddingStart="12dp"
        android:paddingEnd="12dp"
        android:background="@drawable/bg_search_light"
        android:backgroundTint="@null"

        android:drawableStart="@drawable/ic_search_24"
        android:drawablePadding="8dp"/>


    <!-- 列表 + 空提示叠层 -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginTop="10dp">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/skr_mod_market_recycler_view"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scrollbars="vertical"
            android:overScrollMode="never"/>

        <TextView
            android:id="@+id/empty_tips_tv"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:text="模块市场暂无内容"
            android:gravity="center"
            android:textColor="#9AA0A6"
            android:textStyle="bold"
            android:visibility="gone"/>

        <!-- 网络错误 + 刷新按钮（默认隐藏） -->
        <LinearLayout
            android:id="@+id/error_container"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:gravity="center"
            android:padding="16dp"
            android:visibility="gone">

            <TextView
                android:id="@+id/error_tips_tv"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="网络请求失败，请检查网络后重试"
                android:textColor="#9AA0A6"
                android:textStyle="bold"
                android:textSize="14sp"
                android:gravity="center" />

            <Button
                android:id="@+id/btn_retry"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="12dp"
                android:text="刷新"
                android:textAllCaps="false"
                android:textSize="14sp"/>
        </LinearLayout>

    </FrameLayout>

</LinearLayout>

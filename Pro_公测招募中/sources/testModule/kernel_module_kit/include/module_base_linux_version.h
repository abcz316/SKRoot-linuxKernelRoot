#pragma once
#include <iostream>
namespace kernel_module {
/***************************************************************************
 * 获取当前Linux内核版本号（三段数字）
 * 返回： “主.次.补丁” 格式的版本号字符串，如 "5.10.82"；获取失败则返回空字符串。
 ***************************************************************************/
std::string get_kernel_version();

/***************************************************************************
 * 判断当前Linux内核版本号是否小于指定版本号
 * 参数: ver 表示要比较的版本号，如 "5.11.0"
 * 返回: true 代表当前版本号小于 ver，否则返回 false
 *
 * 例如：当前版本 "5.10.82" < "5.11.0" 时返回 true，
 *       当前版本 "5.10.82" ≥ "5.10.80" 时返回 false。
 ***************************************************************************/
bool is_kernel_version_less(const char* ver);
}